dist: bionic
language: node_js
node_js: 16.16
install:
    - npm install
    - npm run build
before_deploy:
    - ssh-keyscan -H $HOSTGATOR_HOST >> ~/.ssh/known_hosts
    
deploy:
    - rsync -avce ssh --delete ./dist/ $HOSTGATOR_USER@$HOSTGATOR_HOST:$HOSTGATOR_DIR